{
  "rules": {
    ".read": true,
    "posts": {
      "$posts": {
        ".write": "auth.uid == data.child('creator').val() || auth.uid == newData.child('creator').val() || newData.child('publicEditable').val() == true || data.child('publicEditable').val() == true"
      },
    },

    "tracks": {
      "$track": {
        ".write": "auth.uid == data.child('postCreatorUid').val() || auth.uid == newData.child('postCreatorUid').val() || newData.child('publicEditable').val() == true || data.child('publicEditable').val() == true"
      }
    },


    "customFunctions": {
      "$customFunction": {
        "postCreatorUid": {
          ".write": "auth !== null"
        },
        "editorContent": {
          ".write": "auth !== null"
        },
        "functionPreCheck": {
          ".write": "auth !== null"
        },
        "function": {
          ".write": false
        },
        "illegalTokens": {
          ".write": false
        }
      }
    },
    

    "comments": {
      "$comment": {
        "body": {
          ".validate": "newData.val().length > 2"
        },
        ".validate": "newData.hasChildren(['body','user'])"
      },
      ".write": "auth !== null"
    },
    "users": {
      "$user": {
        ".write": "auth !== null "

      }

    }

  }
}
