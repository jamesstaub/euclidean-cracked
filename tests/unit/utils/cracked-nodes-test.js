import crackedNodes from 'euclidean-cracked/utils/cracked-nodes';
import { module, test } from 'qunit';

module('Unit | Utility | cracked-nodes', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = crackedNodes();
    assert.ok(result);
  });
});
