import euclidean from 'euclidean-cracked/utils/euclidean';
import { module, test } from 'qunit';

module('Unit | Utility | euclidean');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = euclidean();
  assert.ok(result);
});
