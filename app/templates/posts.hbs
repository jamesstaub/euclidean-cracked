{{#edit-post
  model=model
  session=session
  save='save'
  delete='delete'
  createComment='createComment'
  as |post|
}}
  {{add-track post=post session=session onCreateTrack=(route-action 'createTrack')}}
{{/edit-post}}

{{outlet}}
