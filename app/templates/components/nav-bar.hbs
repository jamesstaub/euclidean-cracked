<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" {{action "toggleCollapsed"}}>
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to 'index' class='navbar-brand' }}s{{/link-to}}
    </div>
    <div class="{{if isCollapsed 'collapse'}} navbar-collapse" id="navigationbar">
      <ul class="nav navbar-nav">
        {{#if session.isAuthenticated}}
        <li>
          {{#link-to 'new'}}Add New Post{{/link-to}}
        </li>
        {{/if}}
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#unless session.isAuthenticated}}
        <li><a href="#" {{action onLogin }}>Login</a></li>
        {{else}}
        <li><a href="#" {{action onLogout }}>Logout</a></li>

        <li class="avatar">
          {{log session.currentUser}}
          <img src='{{session.currentUser.photoURL}}' height='50px' width='50px'/>
        </li>
        {{/unless}}
      </ul>
    </div>
  </div>
</nav>

{{yield}}
