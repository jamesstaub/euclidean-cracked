
<h3>{{track.filename}}9</h3>
<div class="drum-file-picker">
  {{!-- <img src={{img}} alt={{track.filename}}> --}}
  {{#each directories as |directory|}}
    <div class="directory-wrapper">
      <ul>
        {{#each directory.choices as |choice|}}
          <li class={{if (eq directory.currentSelection choice) 'selected'}}>
            <button {{action "onSelect" directory choice}}>
              {{#if (eq directory.type "audio")}}
                {{filename-ui choice}}
              {{else}}
                {{choice}}
              {{/if}}
            </button>
          </li>
        {{/each}}
      </ul>
    </div>
  {{/each}}
</div>
